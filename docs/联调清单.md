# 上服务器前联调清单（Ubuntu 22.04）

## 一、本地自检（已完成）

- [x] 后端编译：`go build`，纯静态 16.8 MB
- [x] 前端构建：`npm run build`
- [x] 单元测试：5 个反作弊检测用例全部通过
- [x] 启动冒烟：`/health`、登录、PalDefender 状态、回档端点正常
- [x] Python 解析器：字段扩展完成（instance_id / 天赋 / 灵魂 / platform_id）

## 二、服务器环境准备

```bash
# 游戏服需开启（PalWorldSettings.ini）
RESTAPIEnabled=True
RESTAPIPort=8212
RCONEnabled=True
RCONPort=25575
AdminPassword=<强密码>
```

## 三、配置核对（config.yaml）

| 项 | 期望值 | 验证 |
|---|---|---|
| `web.password` | 非默认强密码 | 浏览器登录 |
| `rest.address/password` | `http://127.0.0.1:8212` + AdminPassword | `curl -u admin:<pw> http://127.0.0.1:8212/v1/api/info` |
| `rcon.address/password` | `127.0.0.1:25575` + AdminPassword | 前端 RCON 页执行 `ShowPlayers` |
| `save.path` | 真实 `Saved` 目录（含 Level.sav） | `ls <path>/Level.sav` |
| `save.decode_path` | `sav_cli` 绝对路径 | `sav_cli --help` |
| `process.mode` | `systemd`/`docker`（回档需要） | `systemctl status palworld` |
| `anticheat.mode` | `external`（默认） | 告警页可见 |

## 四、sav_cli 独立测试

```bash
cd /opt/paladmin
# 安装依赖
pip install -r module/requirements.txt
# 单独解析一个存档（不回传，输出 JSON）
python3 module/sav_cli.py -f /path/to/Level.sav -o test_out
# 检查输出字段
grep -o '"instance_id"' test_out.json | head -3
grep -o '"talent_hp"' test_out.json | head -1
```
期望：看到 `instance_id`、`talent_*`、`soul_*` 字段。

## 五、端到端联调步骤

1. **启动服务**：`systemctl start paladmin`，看 `logs/pst.log`
2. **在线同步**：仪表盘应显示在线人数（60s 内）
3. **存档同步**：等待 `save.sync_interval`（默认 120s），玩家页应出现存档数据
4. **反作弊触发验证**（安全环境）：
   - 用存档编辑器把某测试帕鲁 HP 改到 99999
   - 等待下次存档扫描，告警页应出现 S001
   - 处置动作按配置执行（默认 warn+ban，建议测试时先把 `punish.ban` 设 false）
5. **封禁链路**：玩家详情页点"踢出/封禁"，检查 `banlist` 和游戏内效果
6. **回档链路**：
   - 先手动备份一次
   - 配置好 `process.mode` 后点"回档"
   - 观察日志：停服→rollback 安全备份→解压→启服
7. **Webhook**：配置 `notify.webhook_url`，触发一条告警，验证钉钉/企业微信收到消息

## 六、PalDefender 集成（可选）

仅当游戏服通过 Wine/Proton 安装了 PalDefender：
```yaml
anticheat:
  mode: "integrated"
paldefender:
  enabled: true
  address: "http://127.0.0.1:17993"
  token: "<在 PalDefender/RESTAPI/Tokens 签发>"
```
验证：前端"PalDefender"页应显示绿色"已连接"。

## 七、常见问题

| 现象 | 排查 |
|---|---|
| 在线玩家为空 | REST 地址/密码错误、防火墙、游戏服未开 RESTAPI |
| 存档同步报错 | `save.path` 不对；`sav_cli` 依赖未装；Level.sav 被占用 |
| 封禁无效 | 官方 REST 需要 UserId 为 `steam_` 前缀；检查 RCON/REST 连通 |
| 回档不启服 | `process.mode=noop`；systemd 服务名错误；docker socket 未挂载 |
| 反作弊误封 | 调大阈值、把管理员加入白名单、用"标记误报"并解封 |
